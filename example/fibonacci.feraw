include("../lib/std.feraw");

number = 500;
n1 = 0;
n2 = 1;
nextTerm = 0;
i = -1;

while(less(i, sub(number, 1)))
{
    i = add(i, 1)
    println(n1);
    nextTerm = add(n1, n2);
    n1 = n2;
    n2 = nextTerm;
}

println(n1);