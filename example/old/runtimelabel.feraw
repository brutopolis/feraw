include("../lib/std.feraw");

?(1, afterall); // just to have the label defined at the start

a = 1;
:start;
print("a = ");
println(a);
a = add(a, 1);
?(less(a, 20), start);

print("start label = ");
println(start);

:afterall;
println("skipped everything.");
