include("../lib/std.feraw");

start:

    content = read_file("./example/os.feraw");
    if (not_equals(content, null))
    {
        print("File content read successfully.");
        print("File content read successfully.");
        println(content);
    }
    else
    {
        print("ERROR: could not read file.");
        ?(1, end);
    }

    
end: 
    free(content);